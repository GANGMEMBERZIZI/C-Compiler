{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build scanner",
            "type": "shell",
            "command": "make", // 或者 "gcc -Wall -g -o scanner main.c scan.c"
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": [
                "$gcc"
            ],
            "detail": "Builds the scanner project"
        }
    ]
}
